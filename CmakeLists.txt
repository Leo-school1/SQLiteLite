cmake_minimum_required(VERSION 3.10)
project(SQLiteLite)

set(CMAKE_C_STANDARD 11)

# Add source files
set(SOURCES
    src/main.c
    src/repl.c
)

# Add executable
add_executable(SQLiteLite ${SOURCES})